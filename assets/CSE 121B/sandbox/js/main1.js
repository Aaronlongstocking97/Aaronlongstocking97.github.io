// Changed PI to pi 
const pi = 3.14;
// Changed Const to let
let radius = 3;
let area = 0;
area = radius * radius * pi;
console.log(area);
radius = 4;
area = radius * radius * pi;
console.log(area);
